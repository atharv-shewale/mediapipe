streamlit>=1.28.0
opencv-python-headless>=4.7.0
mediapipe>=0.10.0
numpy>=1.24.0
pillow>=9.0.0
streamlit-webrtc==0.64.5
# Note: `av` (PyAV) is optional and can be difficult to build on Windows.
# We don't pin it here so local installs won't try to compile PyAV.
# If you need full WebRTC support locally, install PyAV manually:
#   pip install av
